# Week 4 Lecture 1

```java
Fraction[] fracArr new Fraction[10];

for (Fraction f: fracArr)
{
	f.setNumer (random number);
	f.setDenom (random number);
}

Arrays.sort(fracArr); // fail at finding .compareTo()
```